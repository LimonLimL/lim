services:
  database:
    image: postgres:16.13-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: office_db
      PGPORT: 5555
    ports:
    - "5655:5555"
    volumes:
    - office_db_data:/var/lib/postgresql


volumes:
  office_db_data: